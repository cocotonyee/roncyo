# Blog content (GoShip contract)

Truth source in production: Supabase `seo_documents` (Hub SEO).  
This folder is **seed / offline fallback**. Same slug → DB wins.

Reserved slugs: `page`, `rss.xml`.

See GoShip `site/templates/goship-starter/content/blog/README.md` for full frontmatter schema.
