{
  "name": "roncyo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w www",
    "build": "npm run build -w www",
    "start": "npm run start -w www",
    "lint": "npm run lint -w www",
    "pack:goship": "node scripts/pack-goship.mjs"
  }
}
